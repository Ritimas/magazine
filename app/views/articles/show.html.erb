<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @article.body %>
</p>

<p>
  <strong>Author:</strong>
  <%= @article.user.name %>
</p>

<%= link_to 'Edit', edit_article_path(@article) if @article.editable_by?(current_user) %> |
<%= link_to 'Back', articles_path %>

<h2>Comments</h2>
<%= render partial: 'comments/show', locals: { comments: @article.comments.where(comment_id: nil) }%>